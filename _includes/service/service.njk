        <template x-if="{{params.formula}}" >
            <article class="flex bg-gray-300 rounded-xl border-gray-600 border p-4 m-4">
                <div class="w-2/3">
                <h3 class="font-bold text-2xl">{{params.service}}</h3>
                <p> {{params.description}}</p>
                </div>
                <div class="w-1/3 flex justify-center">
                <a href="{{params.link}}" class="p-4 bg-ny-blue text-white font-bold rounded-xl"> {{params.linktext}} </a>
                </div>
            </article>
        </template>